//! This module defines everything related to inputs (e.g. parsing) and outputs (e.g. serializing).
//!
//! In particular, it defines data structures `Type` and `Value`.

pub mod parse;
pub mod range;
pub mod serialize;
pub mod types;